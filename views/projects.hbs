<!DOCTYPE html>
<html>
  <meta charset = "utf-8">
</html>
<head>
  <title>{{pageTitle}}</title>
</head>
<body>
  {{> header}}
  <p><b>{{message}}</b></p>
  <ul>
    {{#each projects}}
        <li>{{.}}</li>
    {{/each}}
  </ul>
  {{> footer}}
</body>
